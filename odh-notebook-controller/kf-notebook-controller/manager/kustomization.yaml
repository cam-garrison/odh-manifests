resources:
  - manager.yaml
  - service-account.yaml
  - service.yaml
configMapGenerator:
  - name: config
    literals:
      - USE_ISTIO=true
      # TODO(ossm): use env-vars for this?
      - ISTIO_GATEWAY=opendatahub/odh-gateway
generatorOptions:
  disableNameSuffixHash: true
